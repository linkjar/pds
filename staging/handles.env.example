# Merge into #90's PDS environment (not the public web app's environment).
PDS_HOSTNAME=pds.linkjar.social
PDS_SERVICE_HANDLE_DOMAINS=.linkjar.social
# Optional Caddy environment; upstream must not be exposed on a public port.
PDS_UPSTREAM=pds:3000
