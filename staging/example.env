# Copy outside Git as image.env after #90 has provisioned the staging host.
# Replace DIGEST with the digest from the successful publish job.
PDS_IMAGE=ghcr.io/linkjar/pds@sha256:DIGEST
PDS_ENV_FILE=/etc/linkjar-pds/staging.env
PDS_DATA_PATH=/var/lib/linkjar-pds/staging
